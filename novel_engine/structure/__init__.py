"""Structure engine: computed read-pull and anti-shrink signals.

Lifts the scattered legacy regex checks (next_step_decision chains, one-day
chains, thin world_expansion, pacing loops, growth density) into one typed,
computed model over per-chapter structural records. Positive signals, profile
thresholds, testable — not regex over rolling_plan.yml.
"""

__all__ = ["chapter_meta", "analyze", "legacy_structure"]
